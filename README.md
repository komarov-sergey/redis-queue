# Redis Queue Application

## Configuration

To configure the application, set the necessary environment variables in the `docker-compose.yml` file.

## Running

```
docker compose up --build

```

## Check result

check consumer-1 container file: `/usr/app/result.json`
